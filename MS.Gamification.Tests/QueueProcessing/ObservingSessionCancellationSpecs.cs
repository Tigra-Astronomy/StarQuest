using Machine.Specifications;
using MS.Gamification.BusinessLogic.QueueProcessing;

namespace Namespace
    {
    [Subject(typeof(ObservingSessionCancellation))]
    public class When_processing_a_session_cancellation
        {
        Establish context = () => {  };

        Because of = () => { };

        It should_ = () => { };
        }
    }